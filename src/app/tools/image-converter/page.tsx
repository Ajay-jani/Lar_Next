import { ImageConverter } from '@/components/tools/ImageConverter'

export default function ImageConverterPage() {
  return <ImageConverter />
}

export const metadata = {
  title: 'Image Converter & Filter - Dev Utilities Hub',
  description: 'Convert images between formats and apply professional filters. Supports JPEG, PNG, WebP, BMP, TIFF with advanced editing capabilities.',
}