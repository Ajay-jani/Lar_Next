import { ImageResizer } from '@/components/tools/ImageResizer'

export default function ImageResizerPage() {
  return <ImageResizer />
}

export const metadata = {
  title: 'Image Resizer - Dev Utilities Hub',
  description: 'Resize images with precision, social media presets, and professional quality optimization. Advanced image resizing tool with smart modes and batch processing.',
}